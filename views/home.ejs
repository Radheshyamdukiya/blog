<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Blog</title>
  <style>
    *{margin:0;padding:0;box-sizing:border-box;font-family:'Segoe UI',sans-serif;}

    body{
      background:#fdfdfd;
      color:#333;
      line-height:1.6;
    }

    header{
      background:#fff;
      padding:15px 40px;
      display:flex;
      justify-content:space-between;
      align-items:center;
      box-shadow:0 2px 8px rgba(0,0,0,0.1);
      position:sticky;
      top:0;
      z-index:1000;
    }

    .logo{
      font-size:24px;
      font-weight:bold;
      color:#0066ff;
    }

    nav ul{
      display:flex;
      list-style:none;
      gap:25px;
    }

    nav ul li a{
      text-decoration:none;
      color:#333;
      font-weight:500;
      transition:0.3s;
    }

    nav ul li a:hover{
      color:#0066ff;
    }

    .hero{
      text-align:center;
      padding:60px 20px;
      background:linear-gradient(135deg,#0066ff,#00c6ff);
      color:white;
    }

    .hero h1{font-size:38px;margin-bottom:15px;}
    .hero p{font-size:18px;}

    .container{
      display:grid;
      grid-template-columns:250px 1fr;
      gap:20px;
      padding:40px;
    }

    .sidebar{
      background:#fff;
      padding:20px;
      border-radius:12px;
      box-shadow:0 2px 6px rgba(0,0,0,0.1);
      height:fit-content;
    }

    .sidebar h3{margin-bottom:15px;color:#0066ff;}
    .sidebar ul{list-style:none;}
    .sidebar ul li{margin:12px 0;}
    .sidebar ul li a{text-decoration:none;color:#444;transition:0.3s;}
    .sidebar ul li a:hover{color:#0066ff;}

    .content{
      background:#fff;
      padding:30px;
      border-radius:12px;
      box-shadow:0 2px 6px rgba(0,0,0,0.1);
    }

    .content h2{margin-bottom:20px;color:#0066ff;}
    .content p{font-size:16px;margin-bottom:10px;}

    /* Action Buttons */
    .btn-group{
      margin-top:25px;
      display:flex;
      gap:15px;
      flex-wrap:wrap;
    }

    .btn{
      padding:12px 22px;
      border:none;
      border-radius:8px;
      cursor:pointer;
      font-size:16px;
      font-weight:500;
      transition:0.3s;
      color:white;
    }

    .btn-create{
      background:linear-gradient(135deg,#28a745,#45d67b);
    }
    .btn-create:hover{
      transform:translateY(-3px);
      box-shadow:0 4px 10px rgba(40,167,69,0.3);
    }

    .btn-read{
      background:linear-gradient(135deg,#007bff,#00b4ff);
    }
    .btn-read:hover{
      transform:translateY(-3px);
      box-shadow:0 4px 10px rgba(0,123,255,0.3);
    }

    .btn-edit{
      background:linear-gradient(135deg,#ffc107,#ff9800);
    }
    .btn-edit:hover{
      transform:translateY(-3px);
      box-shadow:0 4px 10px rgba(255,193,7,0.3);
    }

    footer{
      background:#111;
      color:#eee;
      text-align:center;
      padding:25px 10px;
      margin-top:40px;
    }
    footer img{width:140px;margin-top:10px;}

    @media(max-width:900px){.container{grid-template-columns:1fr;}}
    @media(max-width:600px){
      nav ul{display:none;flex-direction:column;background:#fff;position:absolute;top:70px;right:0;width:200px;padding:20px;box-shadow:0 2px 8px rgba(0,0,0,0.1);}
      nav ul.active{display:flex;}
      .menu-toggle{display:block;cursor:pointer;font-size:22px;}
    }
    .menu-toggle{display:none;}
  </style>
</head>
<body>

  <!-- Navbar -->
  <header>
    <div class="logo">MyBlog</div>
    <div class="menu-toggle" onclick="toggleMenu()">☰</div>
    <nav>
      <ul id="menu">
        <li><a href="#">Home</a></li>
        <li><a href="#">About</a></li>
        <li><a href="#">Blog</a></li>
        <li><a href="#">Contact</a></li>
      </ul>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <h1>Welcome <%= user.username %></h1>
    <p>Your personal blogging dashboard</p>
  </section>

  <!-- Layout -->
  <div class="container">
    <!-- Sidebar -->
    <aside class="sidebar">
      <h3>Categories</h3>
      <ul>
        <li><a href="#">Tech</a></li>
        <li><a href="#">Travel</a></li>
        <li><a href="#">Lifestyle</a></li>
        <li><a href="#">Education</a></li>
      </ul>
    </aside>

    <!-- Content -->
    <main class="content">
      <h2>User Information</h2>
      <p><strong>username:</strong> <%= user.username %></p>
      <p><strong>Email:</strong> <%= user.email %></p>

      <!-- Action Buttons -->
      <div class="btn-group">
      <a href="/creat-blog/<%=user.id %>"><button  class="btn btn-create" >➕ Create Blog</button></a>  
       <a href="/show-all-blog"> <button class="btn btn-read">📖 Read Blogs</button></a>
       <a href="/show-my-blog/<%= user.id %>"> <button class="btn btn-edit">✏️ Edit Blog</button></a>
      </div>
    </main>
  </div>
  <footer>
    <p>&copy; 2025 MyBlog | All Rights Reserved</p>
    <img src="https://upload.wikimedia.org/wikipedia/commons/a/ab/Logo_TV_2015.png" alt="Logo">
  </footer>

  <script>
    function toggleMenu(){
      document.getElementById("menu").classList.toggle("active");
    }
  </script>
</body>
</html>
